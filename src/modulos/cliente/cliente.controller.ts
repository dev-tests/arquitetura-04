// Todo módulo deve possuir a sua controller, onde essa será responsável pelo controle do fluxo da funcionalidade
// nela não haverá lógica de negócio, apenas irá receber uma chamada, chamar os métodos que atendam o solicitado
// e retornar o resultado da solicitação

export const criar = async (): Promise<string> => {
    return 'sucesso'
}

export const editar = async (): Promise<string> => {
    return 'sucesso'
}

export const deletar = async (): Promise<string> => {
    return 'sucesso'
}

export const buscar = async (): Promise<string> => {
    return 'sucesso'
}