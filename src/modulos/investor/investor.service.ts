/**
 * Lógica de negócio deve ser adicionada nesse arquivo
 */

const saveOnDb = (cliente) => 'sucesso'
const removeFromDb = (id) => 'sucesso'
const patchOnDb = (id, cliente) => 'sucesso'
const findOnDb = (params) => 'sucesso'
const getFromDb = (id) => 'sucesso'
