/**
 * Lógica de negócio deve ser adicionada nesse arquivo
 */

const salvarNoDb = (cliente) => 'sucesso'
const removerDoDb = (id) => 'sucesso'
const atualizarNoDb = (id, cliente) => 'sucesso'
const buscarNoDb = (params) => 'sucesso'
const selecionarDoDb = (id) => 'sucesso'
